/*public class Practice3 {
    public static void main(String[] args) {
        int a = 1;
        {
            int b = 2;
        }
        int c = a + b; // ОШИБКА КОМПИЛЯЦИИ
    }
}*/

// Результат: Код не скомпилируется.

// Объяснение:
// Переменная b объявлена внутри блока {} и является локальной переменной этого блока
// Область действия переменной b ограничена только тем блоком {}, в котором она объявлена
// При попытке обратиться к переменной b вне этого блока возникает ошибка компиляции: "cannot find symbol"
// Переменная a объявлена в более широкой области видимости, поэтому доступна во всем методе

public class Practice3Correct {
    public static void main(String[] args) {
        int a = 1;
        int b; // объявляем в той же области видимости, что и a
        {
            b = 2; // инициализируем внутри блока
        }
        int c = a + b; // теперь работает
        System.out.println(c); // 3
    }
}
